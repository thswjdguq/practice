var cat = 1;

cat = cat + 1;  // cat 값은 2 가 됨	
document.write(cat);
cat = cat + 1;  // cat 값은 3 가 됨	

cat = 0;    // cat 값에 오류 값이 들어가버렸다고 가정하고	

cat = cat + 1;  // cat 값은 4 가 됨	

document.write(cat);    // cat 값 출력	